import { describe, test, expect } from 'vitest';

import '@/pages/work/_401k.client';

describe('401k.client', () => {
    describe('401k client', () => {
        test('should be defined', () => {
            expect(true).toBe(true);
        });
    });
});
