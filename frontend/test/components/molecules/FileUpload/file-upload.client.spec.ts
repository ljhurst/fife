import { describe, test, expect } from 'vitest';

import '@/components/molecules/FileUpload/file-upload.client';

describe('file-upload.client', () => {
    describe('FileUpload client', () => {
        test('should be defined', () => {
            expect(true).toBe(true);
        });
    });
});
