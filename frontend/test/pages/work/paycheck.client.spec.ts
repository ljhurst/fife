import { describe, test, expect } from 'vitest';

import '@/pages/work/_paycheck.client';

describe('paycheck.client', () => {
    describe('paycheck client', () => {
        test('should be defined', () => {
            expect(true).toBe(true);
        });
    });
});
