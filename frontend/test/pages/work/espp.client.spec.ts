import { describe, test, expect } from 'vitest';

import '@/pages/work/_espp.client';

describe('espp.client', () => {
    describe('ESPP client', () => {
        test('should be defined', () => {
            expect(true).toBe(true);
        });
    });
});
